name: CI

on:
  push:

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: action/checkout@v4

      - name: Install Dependencies
        run: pip install pytest

      - name: Run tests
        run: pytest